{
  "name": "kanban-skill",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun run scripts/regression-test.ts",
    "help": "bun run scripts/help.ts",
    "init": "bun run scripts/init.ts"
  },
  "dependencies": {
    "proper-lockfile": "^4.1.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/proper-lockfile": "latest",
    "typescript": "^5.9.3"
  }
}
